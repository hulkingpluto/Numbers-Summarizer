package com.example;

import java.util.Collection;

public interface NumberSummarizerInterface {
    Collection<Integer> collect(String input);
    String summarizeCollection(Collection<Integer> input);
}
